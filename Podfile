# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TextCycle' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TextCycle
	pod ‘Bond’
	pod ‘ReactiveKit’
	pod ‘Alamofire’
	pod ‘SwiftyJSON’
	pod 'Fabric'
	pod 'Digits'    
	pod 'TwitterCore'

  target 'TextCycleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TextCycleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
